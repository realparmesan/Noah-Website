{
    "title": "{{ .Title }}",
    "match": " {{ .Params.match }}",
    "date" : "{{ .Params.date }}",
    "result": "{{ .Params.result }}",
    "scorers": [
        {{ range .Params.scorers }}
            "name": "{{ .scorer }}",
            "goals": "{{ .goals }}",
        {{ end }}
    ],
    "permalink" : "{{ .Permalink }}"
}