---
label: MatchData
hide_body: true
fields:
- name: date
  type: datetime
  label: Date
  description: Date of Match
  config:
    required: true
    date_format: 
    time_format: 
    display_utc: false
  default: now
- name: result
  type: select
  default: []
  config:
    required: false
    options:
    - Win
    - Loss
    - Draw
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Result
- name: scorers
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: simple
    options:
    - Chris
    - Lance
    - Yarride
  label: Scorers
  description: Scorers in the match
- name: title
  type: text
  config:
    required: true
  label: title
pages:
- data/matchdata/match1.toml
- data/matchdata/match2.toml
